#include <bits/stdc++.h>using namespace std; // n -> friends// k -> bottles -> each bottle has 'l' ml of drink -> (k*l) ml of drink // c -> limes -> 'd' slices each -> total (c*d) slices// p -> grams of salt  /* For each toast-> nl -> ml of drink -> (k*L)/nl -> number of drinks -> x-> np -> grams of salt -> p/np -> y-> 1 slice of lime -> c*d limes -> z*/  // calculation// min(x, y, z)/n int main() {    int n, k, l, c, d, p, nl, np;    cin >> n >> k >> l >> c >> d >> p >> nl >> np;    int x = (k*l)/nl;    int y = p/np;    int z = c*d;    int minimum = min(min(x, y), z);    cout << minimum/n;}